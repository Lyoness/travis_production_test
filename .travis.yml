

dist: trusty

sudo: false

install: true

script: 
- sudo docker run \
  --volume=/:/rootfs:ro \
  --volume=/var/run:/var/run:rw \
  --volume=/sys:/sys:ro \
  --volume=/var/lib/docker/:/var/lib/docker:ro \
  --publish=8080:8080 \
  --detach=true \
  --storage_driver="stdout" \
  --name=cadvisor \
  google/cadvisor:latest


matrix:
   include:
     - sudo: false
       dist: trusty
       language: __connie__
       env: docker dist=Trusty connie
     - sudo: false
       dist: trusty
       language: __garnet__
       env: docker dist=Trusty garnet
     - sudo: false
       dist: trusty  
       language: __amethyst__
       env: docker dist=Trusty amethyst
     - sudo: false
       dist: precise
       language: ruby
       env: docker precise default

